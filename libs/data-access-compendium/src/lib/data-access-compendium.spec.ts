import { dataAccessCompendium } from './data-access-compendium';

describe('dataAccessCompendium', () => {
  it('should work', () => {
    expect(dataAccessCompendium()).toEqual('data-access-compendium');
  });
});
