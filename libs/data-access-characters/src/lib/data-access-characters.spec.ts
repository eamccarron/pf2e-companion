import { dataAccessCharacters } from './data-access-characters';

describe('dataAccessCharacters', () => {
  it('should work', () => {
    expect(dataAccessCharacters()).toEqual('data-access-characters');
  });
});
